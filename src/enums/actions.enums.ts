export const enum CartActionType {
  AddCartProduct = "Add cart product",
  IncrementQuantity = "Imcrement quantity",
  DecrementQuantity = "Decrement quantity",
  RemoveProduct = "Remove cart product",
  SetCart = "Set cart",
}

export const enum AuthInfoActionType {
  SetAuthInfo = "Set auth info",
}
